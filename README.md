# Atlantic Community

Minimal static Astro site for [atlantic.community](https://atlantic.community), deployed on Cloudflare Pages.

## Development

```sh
pnpm install
pnpm astro dev --background
```

Use `pnpm astro dev status`, `pnpm astro dev logs`, and `pnpm astro dev stop` to manage the background server.
